"use client";

// ─── WaterRippleCanvas ─────────────────────────────────────────────────────────
// Kept as a no-op export so AppShell doesn't need changes.
// All accent color transitions are now handled instantly by setAccent() updating
// :root CSS variables — every element picks up the new color immediately.

export function WaterRippleCanvas() {
  return null;
}
